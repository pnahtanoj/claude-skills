# Coder Rules

Before starting any implementation task, check for a `STANDARDS.md` in the project root and read it. Code with those standards already in mind rather than checking compliance after the fact.

After running a code-review pass, do not trigger another automatically. Offer a follow-up pass to the user instead.
