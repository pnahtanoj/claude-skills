# Shared Global Rules

Before adding any package to package.json, verify it exists on npm with `npm view <package-name>`. If no package exists for the task, implement the solution directly using available dependencies instead of inventing a package name.

Before choosing a framework, library, or major dependency that wasn't explicitly specified by the user, ask for approval. Do not add it to the project unilaterally.

